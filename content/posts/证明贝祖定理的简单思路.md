---
title: 证明贝祖定理的简单思路
date: 2020-01-12T02:01:58+05:30
canonicalUrl: https://wenstudy.com/posts/证明贝祖定理的简单思路/
math: true
---

贝祖定理（Bézout's identity）是其他数论重要结论的基石。当 a, b 皆非零时，以下不定式的整数解 x, y 必存在：

$$
ax + by = gcd(a,b)
$$

上述问题等价于，当 \(a, b\) 互质时，即 \(gcd(a, b) = 1\)（通常记为 \(d\)）， 整数解 \(x, y\) 必存在：
$$
ax + by = 1
$$

因为原式可表示为

$$
\begin{aligned}
a = da' \\
b = db'
\end{aligned}
$$

此时 \(a', b'\) 互质，代入原式，两边除以 \(d\)，便得等价形态：

$$
a'x + b'y = 1
$$

_换句话说，为什么在两数互质时，总能找到一组系数，让他们互相抵消，最终剩下1呢？_

## 一个数总是另一个数的线性组合

因为 a 总能表达为 b 的某种线性组合，这里的偏置 r，其实就是余数；设 a 大 b 小：

$$
a = wb + r\quad(1 ≤ r ≤ b-1)
$$

等价于说，a 模 b 等于 r。

![image of abr](/images/证明贝祖定理的简单思路/abr.png "abr")

而 a 乘上 \([1,b-1]\) 中的任一个数，余数依然在 \([1,b-1]\)里，且不可能有碰撞。

因为如果 \(ab'\) 和 \(ab''\) 模 b 同余，即：

$$
ab' ≡ ab''\quad(mod\,b)\quad(1 ≤ b' < b'' ≤ b-1)
$$

则：
$$
b\mid a(b''-b')
$$

因 a, b 互质，只能：
$$
b\mid(b''-b')
$$

而这是不可能的，因为有以下前提：（一个更小的数怎能被一个更大的数整除）
$$
0 < b'' - b' < b
$$

显然，\([1,b-1]\) 个乘数，乘以 a，会一对一映射到 \([1,b-1]\) 个余数里，只会打乱顺序，但无冲突。那么必然有一个乘数，得到余数 1。

## 威尔逊定理

更有趣的结论是，因为 a 是任选的，只要能模 b 后，产生余数 \(r \in \{1,2,\ldots,b-1\}\) 即可。这等于说，不用在意 a，而只看余数 \([1,b-1]\) 本身，就必可以选出两个数 \(r_1, r_2\)，导致相乘后模 b 为 1。

上面已经知道，给定一个 a（等于说给定一个 r），再乘 \(r \in \{1,2,\ldots,b-1\}\) ，只会一对一映射到 \([1,b-1]\)，
